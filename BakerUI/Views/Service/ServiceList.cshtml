@model BakerUI.Dto.ServiceDto.ResultServiceDto

<div class="container-xxl py-6">
    <div class="container">
        <div class="row g-5">

            <!-- SOL -->
            <div class="col-lg-6 wow fadeInUp" data-wow-delay="0.1s">
                <p class="text-primary text-uppercase mb-2">// Hizmetlerimiz</p>
                <h1 class="display-6 mb-4">@Model.Title</h1>
                <p class="mb-5">@Model.Description</p>

                <div class="row gy-5 gx-4">
                    @for (int i = 0; i < Model.Items.Count; i++)
                    {
                        var item = Model.Items[i];

                        <div class="col-sm-6 wow fadeIn" data-wow-delay="@(0.1 + (i * 0.1))s">
                            <div class="d-flex align-items-center mb-3">
                                <div class="flex-shrink-0 btn-square bg-primary rounded-circle me-3">
                                    <i class="fa fa-check text-white"></i>
                                </div>
                                <h5 class="mb-0">@item.Title</h5>
                            </div>

                            <span>@item.Description</span>
                        </div>
                    }
                </div>
            </div>

            <!-- SAĞ -->
            <div class="col-lg-6 wow fadeInUp" data-wow-delay="0.5s">
                <div class="row img-twice position-relative h-100">
                    <div class="col-6">
                        <img class="img-fluid rounded"
                             src="@(string.IsNullOrWhiteSpace(Model.ImageUrl1) ? Url.Content("~/img/service-1.jpg") : Model.ImageUrl1)"
                             alt="">
                    </div>
                    <div class="col-6 align-self-end">
                        <img class="img-fluid rounded"
                             src="@(string.IsNullOrWhiteSpace(Model.ImageUrl2) ? Url.Content("~/img/service-2.jpg") : Model.ImageUrl2)"
                             alt="">
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
